# Server Configuration
PORT=3000

# Authentication Secret
# Change this to a secure random string for production
CLIQ_SECRET=testsecret

# Domain Allowlist (set to true to enforce)
ENFORCE_ALLOWLIST=false

# Comma-separated list of allowed domains (when ENFORCE_ALLOWLIST=true)
# Example: example.com,notion.com,github.com
ALLOWED_DOMAINS=example.com

# Optional: Custom Chromium executable path
# Leave empty to use bundled Playwright Chromium
# CHROMIUM_EXECUTABLE_PATH=/path/to/chromium
